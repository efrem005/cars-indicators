
const config = {
    host: import.meta.env.VITE_HOST_NAME,
    port: import.meta.env.VITE_HOST_PORT,
    protocol: import.meta.env.VITE_HOST_PROTOCOL,
    user: import.meta.env.VITE_HOST_USER,
    pass: import.meta.env.VITE_HOST_PASS,
}

export default config